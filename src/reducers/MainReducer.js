const initialState = {
 tb:''
}

export default function MainReducer(state=initialState,action){
  switch (action.type) {
    case 'dsf':
      return state
      break;
    default:
      return state
  }
}
